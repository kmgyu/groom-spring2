<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goorm.mybatisboard.post.PostMapper">

    <select id="findAll" resultType="goorm.mybatisboard.post.Post">
        SELECT id, title, content, created_at, updated_at
        FROM posts
        ORDER BY created_at DESC
    </select>

    <select id="findAllWithPage" resultType="goorm.mybatisboard.post.Post">
        SELECT id, title, content, created_at, updated_at
        FROM posts
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="findAllWithSearch" resultType="Post">
        SELECT id, title, content, created_at, updated_at
        FROM posts
        <where>
            <if test="keyword != null and keyword.trim() != ''">
                <choose>
                    <when test="searchType == 'title'">
                        LOWER(title) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                    </when>
                    <when test="searchType == 'content'">
                        LOWER(content) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                    </when>
                    <otherwise>
                        (LOWER(title) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                        OR LOWER(content) LIKE LOWER(CONCAT('%', #{keyword}, '%')))
                    </otherwise>
                </choose>
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(id)
        FROM posts
    </select>

    <select id="countAllWithSearch" resultType="int">
        SELECT COUNT(id)
        FROM posts
        <where>
            <if test="keyword != null and keyword.trim() != ''">
                <choose>
                    <when test="searchType == 'title'">
                        LOWER(title) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                    </when>
                    <when test="searchType == 'content'">
                        LOWER(content) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                    </when>
                    <otherwise>
                        (LOWER(title) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
                        OR LOWER(content) LIKE LOWER(CONCAT('%', #{keyword}, '%')))
                    </otherwise>
                </choose>
            </if>
        </where>
    </select>

    <select id="findById" parameterType="long" resultType="goorm.mybatisboard.post.Post">
        SELECT id, title, content, created_at, updated_at
        FROM posts
        WHERE id = #{id}
    </select>

    <insert id="save" parameterType="goorm.mybatisboard.post.Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (title, content, created_at, updated_at)
        VALUES (#{title}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE posts 
        SET title = #{post.title}, 
            content = #{post.content}, 
            updated_at = #{post.updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM posts 
        WHERE id = #{id}
    </delete>

</mapper>