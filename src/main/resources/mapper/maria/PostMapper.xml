<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goorm.mybatisboard.post.mapper.PostMapper">

    <!-- Post Result Map -->
    <resultMap id="PostResultMap" type="goorm.mybatisboard.post.model.Post">
        <id property="seq" column="seq"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="viewCount" column="view_count"/>
        <result property="createdAt" column="created_at"/>

        <!-- User 연관관계 매핑 -->
        <association property="author" javaType="io.goorm.board.entity.User">
            <id property="userSeq" column="user_seq"/>
            <result property="email" column="user_email"/>
            <result property="nickname" column="user_nickname"/>
            <result property="createdAt" column="user_created_at"/>
            <result property="updatedAt" column="user_updated_at"/>
        </association>
    </resultMap>

    <!-- 모든 게시글 조회 (작성자 정보 포함) -->
    <select id="findAll" resultMap="PostResultMap">
        SELECT
            p.seq,
            p.title,
            p.content,
            p.view_count,
            p.created_at,
            u.user_seq,
            u.email as user_email,
            u.nickname as user_nickname,
            u.created_at as user_created_at,
            u.updated_at as user_updated_at
        FROM posts p
             JOIN users u ON p.user_seq = u.user_seq
        ORDER BY p.created_at DESC
    </select>

    <!-- ID로 게시글 조회 (작성자 정보 포함) -->
    <select id="findBySeq" resultMap="PostResultMap">
        SELECT
            p.seq,
            p.title,
            p.content,
            p.view_count,
            p.created_at,
            u.user_seq,
            u.email as user_email,
            u.nickname as user_nickname,
            u.created_at as user_created_at,
            u.updated_at as user_updated_at
        FROM posts p
                 JOIN users u ON p.user_seq = u.user_seq
        WHERE p.seq = #{seq}
    </select>

    <insert id="save" parameterType="goorm.mybatisboard.post.model.Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (title, content, created_at, updated_at)
        VALUES (#{title}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="goorm.mybatisboard.post.model.Post" >
        UPDATE posts
        SET title = #{title},
            content = #{content},
            updated_at = NOW(),
            category_id = #{categoryId},
            status = #{status},
            user_id = #{userId},
            view_count = #{viewCount}
            WHERE id = #{seq}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM posts WHERE id = #{seq}
    </delete>

    <!-- 게시글 삭제 -->
    <delete id="deleteBySeq">
        DELETE FROM posts WHERE seq = #{seq}
    </delete>


    <!-- 조회수 증가 -->
    <update id="incrementViewCount">
        UPDATE posts
        SET view_count = view_count + 1,
            updated_at = NOW()
        WHERE seq = #{seq}
    </update>

    <!-- legacy code (unused) -->
    <select id="findAllWithPage" resultType="goorm.mybatisboard.post.model.Post">
        SELECT seq, title, content, created_at, updated_at
        FROM posts
        ORDER BY created_at DESC
            LIMIT #{offset}, #{size}
    </select>

    <select id="findAllWithSearch" resultType="Post">
        SELECT seq, title, content, created_at, updated_at
        FROM posts
        <where>
            <if test="keyword != null and keyword.trim() != ''">
                <choose>
                    <when test="searchType == 'title'">
                        title LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'content'">
                        content LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <otherwise>
                        (  title LIKE CONCAT('%', #{keyword}, '%')
                        OR content LIKE CONCAT('%', #{keyword}, '%') )
                    </otherwise>
                </choose>
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(seq)
        FROM posts
    </select>

    <select id="countAllWithSearch" resultType="int">
        SELECT COUNT(seq)
        FROM posts
        <where>
            <if test="keyword != null and keyword.trim() != ''">
                <choose>
                    <when test="searchType == 'title'">
                        title LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'content'">
                        content LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <otherwise>
                        (  title LIKE CONCAT('%', #{keyword}, '%')
                        OR content LIKE CONCAT('%', #{keyword}, '%') ))
                    </otherwise>
                </choose>
            </if>
        </where>
    </select>

    <select id="findById" parameterType="long" resultType="goorm.mybatisboard.post.model.Post">
        SELECT seq, title, content, created_at, updated_at, view_count
        FROM posts
        WHERE seq = #{id}
    </select>




</mapper>